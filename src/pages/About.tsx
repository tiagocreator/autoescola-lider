import { Cta, Hero, CompanyInfo } from '../components';

const About = () => {
  return (
    <main>
      <Hero title={'Quem Somos'} />
      <CompanyInfo />
      <Cta />
    </main>
  );
};

export default About;
